name: Build and Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Rust (with wasm32 target)
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          target: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: |
          curl -sSf https://rustwasm.github.io/wasm-pack/installer/init.sh | sh

      - name: Install Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Cache Cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build client and server using Deno task
        run: deno task build

      - name: Run Rust tests
        run: cargo test --all

      - name: Run Deno tests
        run: deno test

  # Optional: Deploy to Deno Deploy
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repo
  #       uses: actions/checkout@v4
  #     - name: Deploy to Deno Deploy
  #       uses: denoland/deployctl@v1
  #       with:
  #         project: your-deno-project-name
  #         entrypoint: run.ts
  #         root: .
  #         token: ${{ secrets.DENO_DEPLOY_TOKEN }}